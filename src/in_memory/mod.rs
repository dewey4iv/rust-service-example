pub mod users {
    use super::*;

    pub mod insert_user {
        use super::*;
    }

    pub mod list_users {
        use super::*;
    }

    pub mod user_by_username {
        use super::*;
    }
}
